//Boolean data type
// Boolean represent a truth value -> true or flase / yes or no

let age = 13;
let isAdult = false;

let newAge = 20;
let Adult = true;

// console.log(Adult);

// String data type
//String are text or sequence of characters

let name = "Aditya Tile";
email = "abc@abc.com";
password = "password";
contact = "12354";
console.log(typeof name); // to find out type of data type
console.log(name + " " + email + " " + password + " " + contact); // this is Concatenation(adding strings together)

console.log(name.length); // to find lenght of string
console.log(name[name.length - 1]); // to find strings last character
console.log(name[0]); // to find strings first character

// Null & undefined data types

// 1. Undefined:
// A variable that has not been assigned a value is of type undefined
// Example:

let year;
console.log(year);

let month;
console.log(month);

// 2.Null
// The null value represents the intentional absence of any object value.
// To be explicitly assigned

let marks = null;
let date = null;

console.log(marks, date);
