// Unary operator in JS

age = age + 1;
age += 1;
age++; // increment

age = age - 1;
age -= 1;
--age; // decrement

// Pre-increment (change, then use)

let age = 10;
let newAge = ++age;

//Post-decrement (use, then change)

let myAge = 20;
let myNewAge = myAge++;
